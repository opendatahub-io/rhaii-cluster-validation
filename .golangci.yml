issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - staticcheck
      text: "SA1019"  # fake.NewSimpleClientset deprecation — NewClientset requires generated apply configs we don't have
